---
phase: 01-ffi-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [build.zig, src/root.zig, src/ffi/c.zig]
autonomous: true

must_haves:
  truths:
    - "Project compiles with 'zig build'"
    - "LinuxCNC HAL headers are accessible via @cImport"
    - "Basic Zig executable runs and exits successfully"
  artifacts:
    - path: "build.zig"
      provides: "Zig build configuration"
      contains: ["b.option", "b.installArtifact"]
    - path: "src/root.zig"
      provides: "Entry point for haltune binary"
      min_lines: 10
    - path: "src/ffi/c.zig"
      provides: "Raw C imports from LinuxCNC HAL headers"
      contains: ["@cImport", "@cInclude"]
  key_links:
    - from: "build.zig"
      to: "src/root.zig"
      via: "b.installArtifact with main executable"
      pattern: "b\\.installArtifact"
    - from: "src/root.zig"
      to: "src/ffi/c.zig"
      via: "import statement for c.zig"
      pattern: "@import\\(.*c\\.zig"
---

<objective>
Create Zig project scaffolding and establish C header imports for LinuxCNC HAL API.

Purpose: This is the foundation that all FFI work builds on. Without proper build configuration and C header access, no FFI calls are possible.

Output: Working Zig project that compiles and imports LinuxCNC HAL C headers.
</objective>

<execution_context>
@/home/robert/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ffi-foundation/01-CONTEXT.md
@.planning/phases/01-ffi-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create build.zig with LinuxCNC HAL linkage</name>
  <files>build.zig</files>
  <action>
Create build.zig with:
1. Standard Zig build structure using std.Build
2. Executable target for haltune binary
3. Link against LinuxCNC HAL library (system library search path)
4. Add include path for LinuxCNC headers (/usr/include/linuxcnc/)
5. Target aarch64-linux for Raspberry Pi 5 compatibility

Use b.systemOption for LinuxCNC path if needed for dev vs production environments.

DO NOT use custom build steps beyond what Zig provides natively.
  </action>
  <verify>zig build --help succeeds without errors</verify>
  <done>build.zig exists and 'zig build --help' outputs valid help text</done>
</task>

<task type="auto">
  <name>Create src/root.zig entry point</name>
  <files>src/root.zig</files>
  <action>
Create src/root.zig as the main entry point:
1. Add pub fn main() !void that prints "haltune FFI layer initialized" and exits
2. Import std for printing
3. Keep it minimal - this is just to verify the build works

This will be replaced with actual HAL initialization in later plans.
  </action>
  <verify>zig build runs and prints "haltune FFI layer initialized"</verify>
  <done>Running ./zig-out/bin/haltune prints the initialization message</done>
</task>

<task type="auto">
  <name>Create src/ffi/c.zig with @cImport for HAL headers</name>
  <files>src/ffi/c.zig</files>
  <action>
Create src/ffi/c.zig to import LinuxCNC HAL C headers:
1. Use @cImport(@cInclude("hal.h")) to pull in HAL API
2. Add build.zig include search path to find hal.h at /usr/include/linuxcnc/
3. Export the imported C namespace as "pub const c = @cImport(...)"
4. Add comment explaining this file contains raw C declarations - NOT safe for direct use

DO NOT wrap any C functions here - this is ONLY for header imports.

If hal.h is not found during build, add explicit include path in build.zig using b.addIncludePath.
  </action>
  <verify>zig build compiles successfully (c.zig is imported even if not yet used)</verify>
  <done>c.zig compiles and can be imported by other Zig files</done>
</task>

</tasks>

<verification>
1. Run 'zig build' - must complete without errors
2. Run './zig-out/bin/haltune' - must print initialization message
3. Check that src/ffi/c.zig contains @cImport with hal.h
</verification>

<success_criteria>
1. Zig project compiles successfully with 'zig build'
2. Running the binary prints a message proving it linked
3. c.zig successfully imports LinuxCNC HAL headers (verified by compilation)
4. Project structure is in place: build.zig, src/root.zig, src/ffi/c.zig
</success_criteria>

<output>
After completion, create `.planning/phases/01-ffi-foundation/01-01-SUMMARY.md` with:
- Actual LinuxCNC header path discovered
- Any build configuration tweaks needed (include paths, library paths)
- Zig version used
- Verification that compilation succeeded
</output>
