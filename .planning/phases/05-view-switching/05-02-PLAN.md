---
phase: 05-view-switching
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified: ["src/tui/layout.zig"]
autonomous: true

must_haves:
  truths:
    - "Layout function checks model.current_view to determine rendering mode"
    - "Tree-only mode renders tree view at full width"
    - "Table-only mode renders table view at full width"
    - "Each view mode renders help text at bottom"
    - "Help text shows dynamic hint based on current view mode (Table View / Tree View)"
    - "Switching views preserves tree and table widget state (no reset)"
  artifacts:
    - path: "src/tui/layout.zig"
      provides: "Conditional layout rendering based on view mode"
      contains: "switch (self.current_view)"
    - path: "src/tui/layout.zig"
      provides: "Dynamic help text with view-specific hints"
      contains: "createHelpText"
  key_links:
    - from: "src/tui/layout.zig"
      to: "Model.current_view"
      via: "drawTwoPanelLayout function"
      pattern: "self.current_view"
    - from: "src/tui/layout.zig"
      to: "ViewMode enum"
      via: "switch statement for conditional rendering"
      pattern: "switch \\(self.current_view\\)"
---

<objective>
Refactor layout.zig to support conditional rendering between tree-only and table-only views at full width. Update help text to show dynamic hint ("Table View" or "Tree View") based on current mode.

Purpose: Complete single-panel view switching by making layout rendering responsive to current_view state.

Output: Conditional drawTwoPanelLayout function (two cases) and dynamic createHelpText function.
</objective>

<execution_context>
@/home/robert/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-view-switching/05-CONTEXT.md
@.planning/phases/05-view-switching/05-RESEARCH.md
@.planning/phases/05-view-switching/05-01-SUMMARY.md

# Reference implementation
@src/tui/layout.zig
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor drawTwoPanelLayout for conditional rendering</name>
  <files>src/tui/layout.zig</files>
  <action>
    Refactor drawTwoPanelLayout() to conditionally render based on self.current_view (two modes only):

    1. Import ViewMode at top of file (add after Model import):
       ```zig
       const ViewMode = @import("model.zig").ViewMode;
       ```

    2. Replace the existing two-panel layout with a switch statement:
       - Add "switch (self.current_view) {" after the save_dialog_visible check
       - Add .tree_only case (full width tree, help text)
       - Add .table_only case (full width table, help text)

    3. Each case should:
       - Create appropriate panel surfaces using existing createLeftPanel/createRightPanel
       - Allocate children array (2 children: view + help text)
       - Position surfaces correctly (origin)
       - Return Surface with children

    4. For .tree_only:
       - Use createLeftPanel() with max.width
       - 2 children: tree + help text

    5. For .table_only:
       - Use createRightPanel() with max.width
       - 2 children: table + help text

    DO NOT modify createLeftPanel or createRightPanel - they already work correctly.

    Reference RESEARCH.md Pattern 2 for full example (adapted for two modes).
  </action>
  <verify>grep -A5 "switch (self.current_view)" src/tui/layout.zig shows two cases (.tree_only, .table_only)</verify>
  <done>Layout renders differently based on current_view mode (tree or table at full width)</done>
</task>

<task type="auto">
  <name>Task 2: Update createHelpText for dynamic view hints</name>
  <files>src/tui/layout.zig</files>
  <action>
    Modify createHelpText() to accept ViewMode parameter and return dynamic help text:

    1. Change function signature to accept view_mode:
       ```zig
       fn createHelpText(ctx: vxfw.DrawContext, view_mode: ViewMode) std.mem.Allocator.Error!vxfw.Surface
       ```

    2. Build help string dynamically based on view_mode:
       - Base help: "Enter=Edit/Toggle, /=Search, t=Filter Type, c=Filter Comp"
       - View hint (based on mode):
         * .tree_only: ", Ctrl+T=Table View"
         * .table_only: ", Ctrl+T=Tree View"
       - Suffix: ", Ctrl+C=Quit"

    3. Use allocPrint to build string (ctx.arena allocator):
       ```zig
       const view_hint = switch (view_mode) {
           .tree_only => "Table View",
           .table_only => "Tree View",
       };
       const help_str = try std.fmt.allocPrint(ctx.arena, "Enter=Edit/Toggle, /=Search, t=Filter Type, c=Filter Comp, Ctrl+T={s}, Ctrl+C=Quit", .{view_hint});
       ```

    4. Update both call sites in drawTwoPanelLayout to pass self.current_view:
       - .tree_only case: "try createHelpText(ctx, .tree_only)"
       - .table_only case: "try createHelpText(ctx, .table_only)"

    Reference RESEARCH.md Pattern 3 and Example 4 (adapted for two modes).
  </action>
  <verify>grep "allocPrint.*Ctrl+T=" src/tui/layout.zig shows dynamic help string</verify>
  <done>Help text shows mode-specific hint for Ctrl+T action (Table View / Tree View)</done>
</task>

</tasks>

<verification>
Build succeeds with no errors:
```bash
zig build 2>&1 | head -20
```

Test that switching views works:
1. Launch application
2. Press Ctrl-t multiple times
3. Verify layout changes: tree only -> table only -> tree only (two-mode cycle)
4. Verify help text updates to show next action
</verification>

<success_criteria>
1. Code compiles without errors
2. drawTwoPanelLayout has switch statement on self.current_view
3. Both view modes render correctly (tree_only at full width, table_only at full width)
4. Help text is dynamic and shows correct hint for each mode
5. Widget state persists across view switches (tree expands, search, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/05-view-switching/05-02-SUMMARY.md`
</output>
