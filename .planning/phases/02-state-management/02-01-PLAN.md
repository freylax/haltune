---
phase: 02-state-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/cache.zig
autonomous: true

must_haves:
  truths:
    - "Multiple TUI threads can read pin/signal/parameter values concurrently without blocking each other"
    - "Refresh thread can update cached values while TUI reads are in progress (with proper synchronization)"
    - "State cache can store all three HAL data types (pins, signals, parameters) with name-based lookup"
    - "Cache operations are thread-safe with RwLock preventing data corruption"
    - "Cache returns NotFound error when accessing non-existent items"
  artifacts:
    - path: "src/state/cache.zig"
      provides: "StateStore with RwLock-protected HashMaps for pins/signals/params"
      min_lines: 100
      exports: ["StateStore", "init", "deinit", "getPin", "updatePin", "listPins", "getSignal", "updateSignal", "listSignals", "getParam", "updateParam", "listParams"]
  key_links:
    - from: "src/state/cache.zig"
      to: "src/ffi/safe.zig"
      via: "imports HalError for error handling"
      pattern: "const HalError = @import.*errors"
    - from: "src/state/cache.zig"
      to: "std.Thread.RwLock"
      via: "uses RwLock for concurrent read protection"
      pattern: "std\.Thread\.RwLock"
    - from: "src/state/cache.zig"
      to: "std.StringHashMap"
      via: "uses StringHashMap for O(1) name-based lookups"
      pattern: "std\.StringHashMap"
---

<objective>
Create a thread-safe state cache using RwLock and StringHashMap to store HAL pin/signal/parameter values, enabling concurrent reads from TUI while blocking exclusive writes from refresh thread.

Purpose: Phase 2 requires central state storage that multiple TUI components can read simultaneously while a refresh thread updates values. Without this foundation, thread-safe state management is impossible.

Output: src/state/cache.zig with StateStore struct providing get/update/list operations for pins, signals, and parameters.
</objective>

<execution_context>
@/home/robert/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-management/02-RESEARCH.md
@src/ffi/safe.zig
@src/ffi/errors.zig
</context>

<tasks>

<task type="auto">
  <name>Create StateStore struct with RwLock and HashMaps</name>
  <files>src/state/cache.zig</files>
  <action>
    Create src/state/cache.zig with:

    1. Import std and HalError:
    ```zig
    const std = @import("std");
    const HalError = @import("../ffi/errors.zig").HalError;
    ```

    2. Define HalValue union for storing typed values (bit, float, s32, u32):
    ```zig
    pub const HalValue = union(enum) {
        bit: bool,
        float: f64,
        s32: i32,
        u32: u32,
    };
    ```

    3. Create StateStore struct with:
    - allocator: std.mem.Allocator
    - pins: std.StringHashMap(HalValue)
    - signals: std.StringHashMap(HalValue)
    - params: std.StringHashMap(HalValue)
    - rwlock: std.Thread.RwLock

    4. Implement init(allocator) and deinit() functions

    5. Use RwLock pattern from RESEARCH.md Pattern 1:
    - Read operations: lockShared(), defer unlockShared()
    - Write operations: lock(), defer unlock()

    DO NOT call HAL functions while holding rwlock - this prevents deadlock per RESEARCH.md Pitfall 1.
  </action>
  <verify>zig build-lib -femit-bin=zig-cache/lib/state_cache.o src/state/cache.zig compiles without errors</verify>
  <done>StateStore struct exists with init/deinit, three StringHashMaps (pins/signals/params), and RwLock for synchronization</done>
</task>

<task type="auto">
  <name>Implement get/update operations for each data type</name>
  <files>src/state/cache.zig</files>
  <action>
    Add get and update functions for pins, signals, and params:

    1. getPin(name) !HalValue
    - Acquire shared lock
    - Return value from pins HashMap or error.NotFound

    2. updatePin(name, value) !void
    - Acquire exclusive lock
    - Insert/update value in pins HashMap

    3. getSignal(name) !HalValue
    - Same pattern as getPin but for signals HashMap

    4. updateSignal(name, value) !void
    - Same pattern as updatePin but for signals HashMap

    5. getParam(name) !HalValue
    - Same pattern as getPin but for params HashMap

    6. updateParam(name, value) !void
    - Same pattern as updatePin but for params HashMap

    Follow RESEARCH.md Pattern 1 example for RwLock usage.

    Return error.NotFound when key not found in HashMap.
  </action>
  <verify>zig build-lib -femit-bin=zig-cache/lib/state_cache.o src/state/cache.zig compiles, all 6 functions defined</verify>
  <done>Each of the three data types (pins, signals, params) has get and update functions with proper RwLock locking</done>
</task>

<task type="auto">
  <name>Implement list operations for snapshotting keys</name>
  <files>src/state/cache.zig</files>
  <action>
    Add list functions that return snapshots of all keys (for TUI enumeration):

    1. listPins(allocator) ![][]const u8
    - Acquire shared lock
    - Copy all pin keys to ArrayList
    - Release lock and return owned slice

    2. listSignals(allocator) ![][]const u8
    - Same pattern for signals

    3. listParams(allocator) ![][]const u8
    - Same pattern for params

    Follow RESEARCH.md Pitfall 3 safe pattern: snapshot keys while holding lock, iterate after release.

    Use std.ArrayList to collect keys, return toOwnedSlice().

    Caller owns returned memory and must free.
  </action>
  <verify>zig build-lib -femit-bin=zig-cache/lib/state_cache.o src/state/cache.zig compiles, all 3 list functions defined</verify>
  <done>Each of the three data types has a list function that returns an owned slice of keys</done>
</task>

</tasks>

<verification>
Overall phase checks:
1. zig build-lib src/state/cache.zig compiles without errors
2. StateStore has init(allocator) and deinit() functions
3. StateStore has get/update/list operations for pins, signals, and params
4. All operations use RwLock correctly (shared for reads, exclusive for writes)
5. list operations snapshot keys before releasing lock (no iterator held across lock boundary)
</verification>

<success_criteria>
State cache successfully stores HAL values with thread-safe concurrent reads. Multiple threads can read simultaneously without blocking each other. Write operations block readers but complete atomically. All operations return appropriate errors (NotFound for missing keys).
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-management/02-01-SUMMARY.md`
</output>
