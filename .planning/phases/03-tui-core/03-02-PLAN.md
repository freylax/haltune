---
phase: 03-tui-core
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/tui/widgets/tree_view.zig
  - src/tui/model.zig
  - src/tui/layout.zig
autonomous: true

must_haves:
  truths:
    - "Left panel displays tree of HAL components, pins, signals, and parameters"
    - "Tree nodes can be expanded and collapsed with Enter or arrow keys"
    - "Tree nodes have checkboxes to select items for display in data table"
    - "Checked items are tracked in Model state"
    - "User can navigate tree with arrow keys"
  artifacts:
    - path: "src/tui/widgets/tree_view.zig"
      provides: "Tree widget with checkbox selection"
      min_lines: 150
      exports: ["TreeView", "Node"]
    - path: "src/tui/model.zig"
      provides: "Tree state (expanded nodes, checked items)"
      contains: "expanded_nodes"
    - path: "src/tui/layout.zig"
      provides: "Wires TreeView into left panel"
      contains: "tree_view"
  key_links:
    - from: "src/tui/widgets/tree_view.zig"
      to: "src/state/cache.zig"
      via: "StateStore.listPins/listSignals/listParams"
      pattern: "listPins|listSignals|listParams"
    - from: "src/tui/model.zig"
      to: "src/tui/widgets/tree_view.zig"
      via: "@import"
      pattern: "TreeView"
    - from: "src/tui/layout.zig"
      to: "src/tui/widgets/tree_view.zig"
      via: "tree_view.draw"
      pattern: "tree_view.*draw"
---

<objective>
Implement tree navigation widget for browsing HAL component hierarchy with checkbox item selection.

Purpose: Provide the left panel of the two-panel layout with a tree view showing HAL components (as parent nodes) and their pins/signals/params (as child nodes). Users can expand/collapse nodes and select items with checkboxes for display in the data table.

Output: Functional tree widget that reads HAL data from StateStore, displays hierarchy, tracks expanded/collapsed state, and tracks checked items for table display.
</objective>

<execution_context>
@/home/robert/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tui-core/03-RESEARCH.md
@.planning/phases/03-tui-core/03-01-SUMMARY.md

# Prior Plan Context

Plan 03-01 created:
- TUI module structure (src/tui/)
- Two-panel layout in layout.zig
- Model struct with basic vxfw integration
- Vaxis dependency in build.zig

Key patterns from 03-01:
- Use vxfw.Widget with typeErasedEventHandler and typeErasedDrawFn
- Use ctx.arena for temporary allocations
- Call ctx.consumeAndRedraw() to trigger redraw

# StateStore API Available

- listPins(allocator): ![][]const u8
- listSignals(allocator): ![][]const u8
- listParams(allocator): ![][]const u8
- getPin(name): !HalValue
- getSignal(name): !HalValue
- getParam(name): !HalValue

# Research Context

RESEARCH.md Pitfall 5: Tree View State Management
- Store tree state in Model as HashMaps (expanded_nodes, checked_items)
- Draw function renders state, doesn't modify
- Event handlers modify state, then request redraw
</context>

<tasks>

<task type="auto">
  <name>Create TreeView widget with node structure</name>
  <files>src/tui/widgets/tree_view.zig</files>
  <action>
    Create src/tui/widgets/tree_view.zig with tree widget:

    1. Import dependencies:
    ```zig
    const std = @import("std");
    const vxfw = @import("vaxis").vxfw;
    const StateStore = @import("../../state/cache.zig").StateStore;
    ```

    2. Define Node struct for tree hierarchy:
    - name: []const u8 (node label)
    - item_type: enum { component, pin, signal, param }
    - full_name: []const u8 (HAL item name for cache lookup)
    - children: std.ArrayList(*Node) (null if leaf)
    - expanded: bool (only for component nodes)
    - parent: ?*Node

    3. Define TreeView struct:
    - allocator: std.mem.Allocator
    - store: *StateStore
    - root: std.ArrayList(*Node) (top-level component nodes)
    - expanded_nodes: std.StringHashMap(void) (tracks expanded state)
    - checked_items: std.StringHashMap(void) (tracks selected items)

    4. Implement init(allocator, store):
    - Initialize TreeView
    - Call buildTree() to populate from StateStore

    5. Implement buildTree():
    - Call store.listPins(), listSignals(), listParams()
    - Parse names to extract component prefix (e.g., "motion" from "motion.digital-in-00")
    - Group items by component
    - Create component nodes as parents, HAL items as children
    - Use ctx.arena for temporary allocations during grouping

    DO NOT:
    - Implement draw() yet (next task)
    - Implement event handlers yet (next task)
    - Parse riocore config (out of scope for v1)

    Reference: RESEARCH.md Pitfall 5 for state storage pattern
  </action>
  <verify>grep -n "Node\|TreeView\|buildTree" src/tui/widgets/tree_view.zig shows struct definitions</verify>
  <done>Node and TreeView structs defined with component hierarchy grouping logic</done>
</task>

<task type="auto">
  <name>Implement TreeView draw function with checkboxes</name>
  <files>src/tui/widgets/tree_view.zig</files>
  <action>
    Add typeErasedDrawFn to TreeView:

    1. Implement widget() method returning vxfw.Widget

    2. Implement typeErasedDrawFn(ptr, ctx):
    - Cast ptr to *TreeView
    - Calculate available rows (ctx.max.size().?.height - 2 for borders)
    - Build render list of visible nodes (respecting expand/collapse state)
    - Use ctx.arena for temporary string allocations

    3. For each visible node, render:
    - [ ] or [x] for checkbox (check checked_items HashMap)
    - [+ ] or [- ] for expand/collapse indicator (component nodes only)
    - Indentation based on depth (2 spaces per level)
    - Node name

    4. Create vxfw.Surface with:
    - size: calculated from content dimensions
    - buffer: rendered text with checkboxes and indicators
    - Use vxfw.Text widgets for each line

    DO NOT:
    - Implement scrolling yet (assume items fit in panel)
    - Handle mouse clicks yet (keyboard navigation only)

    Reference: RESEARCH.md Example 3 "Text Widget with Styling"
  </action>
  <verify>grep -n "typeErasedDrawFn\|checkbox\|expanded" src/tui/widgets/tree_view.zig shows draw implementation</verify>
  <done>TreeView renders tree with checkboxes and expand/collapse indicators</done>
</task>

<task type="auto">
  <name>Implement TreeView event handlers for navigation</name>
  <files>src/tui/widgets/tree_view.zig</files>
  <action>
    Add typeErasedEventHandler to TreeView:

    1. Implement typeErasedEventHandler(ptr, ctx, event):
    - Cast ptr to *TreeView
    - Handle .key_press events

    2. Navigation handling:
    - Arrow Up/Down: Move cursor between visible nodes
    - Enter: Toggle expand/collapse for component nodes, toggle checkbox for leaf nodes
    - Space: Toggle checkbox for current node

    3. State updates:
    - Update expanded_nodes HashMap on expand/collapse
    - Update checked_items HashMap on checkbox toggle
    - Call ctx.consumeAndRedraw() after state change

    4. Track cursor position:
    - Add cursor_index field to TreeView (index into visible_nodes)
    - Update cursor_index on arrow key navigation
    - Keep cursor_index bounded to visible_nodes.len

    DO NOT:
    - Handle page up/down yet (basic navigation only)
    - Implement mouse navigation (keyboard only per TUI-03)

    Reference: RESEARCH.md Pitfall 5 - event handlers modify state, draw renders state
  </action>
  <verify>grep -n "typeErasedEventHandler\|key_press\|consumeAndRedraw" src/tui/widgets/tree_view.zig shows event handling</verify>
  <done>TreeView handles arrow key navigation, Enter to expand/collapse, Space to toggle checkboxes</done>
</task>

<task type="auto">
  <name>Integrate TreeView into Model and layout</name>
  <files>src/tui/model.zig, src/tui/layout.zig</files>
  <action>
    Update Model and layout to use TreeView:

    1. In src/tui/model.zig:
    - Add tree_view: TreeView field
    - Update init() to initialize tree_view with store
    - Add getCheckedItems() method to return list of checked item names

    2. In src/tui/layout.zig drawTwoPanelLayout():
    - Call self.tree_view.draw(ctx.withConstraints(...)) for left panel
    - Pass left_width and max.height for constraints
    - Create SubSurface with origin at .{ .row = 0, .col = 0 }
    - Keep right panel as placeholder (plan 03-03 will add table)

    3. Update imports in layout.zig:
    - Add const TreeView = @import("widgets/tree_view.zig").TreeView;

    DO NOT:
    - Filter data table by checked items yet (plan 03-03)
    - Start RefreshThread yet (plan 03-03)

    Reference: RESEARCH.md Pattern 2 for SubSurface creation
  </action>
  <verify>grep -n "tree_view\|TreeView" src/tui/model.zig src/tui/layout.zig shows integration</verify>
  <done>TreeView integrated into left panel, displays HAL component hierarchy</done>
</task>

</tasks>

<verification>
1. Tree view displays in left panel (30% width)
2. Component nodes show with [+]/[-] expand indicators
3. Pins/signals/params shown as children under components
4. Arrow keys navigate between nodes
5. Enter expands/collapses component nodes
6. Space toggles checkboxes
7. Checked items tracked in Model state
</verification>

<success_criteria>
1. TreeView widget created and integrated
2. Tree reads from StateStore (pins, signals, params)
3. Component hierarchy displayed correctly
4. Navigation works (arrow keys, Enter for expand/collapse)
5. Checkboxes track selected items
6. Left panel renders tree without overflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-core/03-02-SUMMARY.md` with:
- Phase metadata
- TreeView implementation details
- Component hierarchy parsing notes
- Navigation behavior documented
- Next steps for plan 03-03
</output>
