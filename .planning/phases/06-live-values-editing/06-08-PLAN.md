---
phase: 06-live-values-editing
plan: 08
type: execute
wave: 1
depends_on: []
files_modified: [src/tui/widgets/tree_view.zig]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Tree view value edits persist to HAL via FFI write calls"
    - "Store cache and HAL value stay synchronized after edit"
  artifacts:
    - path: "src/tui/widgets/tree_view.zig"
      provides: "FFI write calls in edit mode confirm handler"
      min_lines: 20
  key_links:
    - from: "src/tui/widgets/tree_view.zig"
      to: "src/ffi/safe.zig"
      via: "pinBitSet/pinFloatSet/pinS32Set/pinU32Set FFI calls"
      pattern: "safe\\.pin(Bit|Float|S32|U32)Set"
---

<objective>
Add FFI write calls to tree view value editing so edited values persist to the HAL system instead of only updating the StateStore cache.

Purpose: Close verification gap where tree view editing (lines 904-954) only calls store.updatePin/updateSignal/updateParam without writing to HAL. The FFI pin*Set functions already exist in safe.zig but are not called by the edit handler.

Output: Tree view value edits that persist to HAL and remain visible after refresh cycles.
</objective>

<execution_context>
@/home/robert/.claude/get-shit-done/workflows/execute-plan.md
@/home/robert/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-live-values-editing/06-live-values-editing-VERIFICATION.md
@.planning/phases/06-live-values-editing/06-02-SUMMARY.md
@.planning/PROJECT.md

# Verification Gap Details

From VERIFICATION.md gap for truth "Value changes reflect immediately in HAL and update displayed value":

**Gap location:** tree_view.zig lines 904-954 (edit mode confirm handler)
**Issue:** Code only calls store.updatePin/updateSignal/updateParam (line 944-948) with no FFI write calls
**Missing:** FFI halPinSet* calls to actually write the value to HAL

**Existing FFI functions in safe.zig:**
- pinBitSet (line 387)
- pinFloatSet (line 407)
- pinS32Set (line 425)
- pinU32Set (line 443)

**Pattern from data_table.zig writeValue function (lines 445-469):**
```zig
.pin => {
    const pin_ptr = try self.getPinPointer(item.name);
    switch (value) {
        .bit => |v| try safe.pinBitSet(@ptrCast(@alignCast(@constCast(pin_ptr))), v),
        .float => |v| try safe.pinFloatSet(@ptrCast(@alignCast(@constCast(pin_ptr))), v),
        .s32 => |v| try safe.pinS32Set(@ptrCast(@alignCast(@constCast(pin_ptr))), v),
        .u32 => |v| try safe.pinU32Set(@ptrCast(@alignCast(@constCast(pin_ptr))), v),
    }
},
```

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FFI write calls to tree view edit confirm handler</name>
  <files>src/tui/widgets/tree_view.zig</files>
  <action>
    In the edit mode confirm handler (around line 904-954), after parsing the new_value and before calling store.updatePin/updateSignal/updateParam:

    1. Import safe FFI module at top of file if not already imported: `const safe = @import("../../ffi/safe.zig");`

    2. Add a helper function `getPinPointer` similar to data_table.zig (lines 418-428):
       - Takes []const u8 name parameter
       - Allocates null-terminated name string with self.allocator
       - Calls safe.halprFindPinByName
       - Returns pin pointer or error.PinNotFound

    3. In the Enter confirm handler (after parsing new_value, around line 943), add FFI write calls BEFORE store updates:
       - For .pin items: get pin pointer, then call appropriate pin*Set function based on value type
       - For .param items: skip FFI write (setParam* functions return InitFailed - params are read-only in ULAPI)
       - For .signal items: skip FFI write (signals are read-only, value comes from linked pins)

    4. Handle FFI write errors gracefully:
       - Log FFI errors to stderr (std.debug.print)
       - Stay in edit mode on FFI error (don't exit edit mode, let user try again)
       - Only exit edit mode if both FFI write AND store update succeed

    IMPORTANT: Call FFI write BEFORE store.updatePin to ensure HAL value is written before cache update matches it.

    Do NOT modify the signal editing logic (Ctrl+S) - that already has FFI calls (halSignalNew, halLink, halSignalDelete).

    Do NOT modify BIT toggle logic - it also needs FFI write added (pinBitSet call after toggling).
  </action>
  <verify>
    grep -n "safe\\.pin(Bit|Float|S32|U32)Set" src/tui/widgets/tree_view.zig
    zig ast-check src/tui/widgets/tree_view.zig
  </verify>
  <done>
    Tree view edit confirm handler calls FFI pin*Set functions to write values to HAL before updating store cache. Edits persist to HAL system and survive refresh cycles.
  </done>
</task>

</tasks>

<verification>
Overall phase gap closure verification:

1. Search for FFI write calls in tree_view.zig: grep should show pinBitSet/pinFloatSet/pinS32Set/pinU32Set calls
2. Verify ast-check passes with no syntax errors
3. Confirm edit handler logic: FFI write happens before store update
4. Confirm error handling: FFI errors are logged and don't crash
</verification>

<success_criteria>
1. Tree view value edits call FFI write functions to persist values to HAL
2. FFI write calls happen before store cache updates
3. FFI write errors are logged to stderr and handled gracefully
4. Code compiles without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-live-values-editing/06-08-SUMMARY.md`
</output>
