# Milestone v0.4: Configuration & Editing

**Status:** ✅ SHIPPED 2026-01-29
**Phases:** 1-4
**Total Plans:** 16

## Overview

Build a TUI-based LinuxCNC HAL manager by first establishing safe FFI bindings to the LinuxCNC HAL C API, then layering thread-safe state management, responsive Vaxis-based TUI for HAL inspection and manipulation, bookmark functionality, and extensible plugin architecture. The journey progresses from low-level foundations (FFI, state caching) to user-facing features (browser, editing) to extensibility (plugins), culminating in performance optimization and polish for Raspberry Pi 5 deployment.

Milestone v0.4 completes the Configuration & Editing phase, providing runtime HAL manipulation capabilities including signal creation, pin linking, and configuration export.

## Phases

### Phase 1: FFI Foundation

**Goal**: Safe Zig wrappers for LinuxCNC HAL C API functions with correct struct alignment, memory management, and version compatibility

**Depends on**: Nothing (first phase)

**Requirements**: FFI-01, FFI-02, FFI-03, FFI-04, FFI-05

**Success Criteria** (what must be TRUE):
1. Zig code successfully calls LinuxCNC HAL C API functions (hal_init, hal_comp_name, etc.) with proper type conversions between Zig and C types
2. FFI layer compiles and runs on ARM64 (Raspberry Pi 5) without struct alignment errors or memory corruption
3. All memory allocated by C functions is properly freed (verified by valgrind or Zig's general purpose allocator leak detection)
4. HAL mutex lock/unlock is correctly paired for all write operations (no deadlocks or data races in multi-threaded scenarios)
5. Code compiles against both LinuxCNC 2.9.7+ and 2.10 APIs without breaking changes

**Plans:** 3 plans in 3 waves

Plans:
- [x] 01-01-PLAN.md — Create build.zig, src/root.zig, src/ffi/c.zig (project scaffolding and C header imports)
- [x] 01-02-PLAN.md — Create src/ffi/types.zig, src/ffi/errors.zig, src/ffi/safe.zig (types, errors, and init/exit wrappers)
- [x] 01-03-PLAN.md — Pin wrapper functions with mutex locking and unit tests with leak detection

**Details:**

Established safe Zig bindings to LinuxCNC HAL C API with proper FFI layer including:
- C header imports via @cImport for hal.h types and functions
- Extern struct definitions with compile-time size assertions for ARM64 compatibility
- Error mapping from LinuxCNC error codes to specific Zig error types
- Mutex-locking wrappers for write operations (hal_pin_*, hal_param_*)
- Lock-free wrappers for read operations
- Memory ownership documentation at FFI boundaries

---

### Phase 2: State Management

**Goal**: Thread-safe central state store that caches HAL data and provides reactive updates to the TUI

**Depends on**: Phase 1

**Requirements**: STATE-01, STATE-02, STATE-03, STATE-04, STATE-05

**Success Criteria** (what must be TRUE):
1. State cache successfully stores current values of all HAL pins, signals, and parameters and refreshes at configured rate (default 100ms)
2. State manager handles dynamic HAL changes (components loading/unloading) without crashing or corrupting cache
3. Multiple threads can read state concurrently while refresh thread writes without race conditions or data corruption
4. TUI components receive state change notifications and can subscribe to specific items (e.g., "notify me when pid.0.P changes")
5. Application runs smoothly without blocking HAL refresh or causing real-time thread starvation

**Plans**: 5 plans in 3 waves

Plans:
- [x] 02-01-PLAN.md — Thread-safe state cache with RwLock and StringHashMap
- [x] 02-02-PLAN.md — HAL refresh thread with configurable polling interval
- [x] 02-03-PLAN.md — Change notification pubsub system for TUI updates
- [x] 02-04-PLAN.md — Signal and parameter refresh implementation (gap closure)
- [x] 02-05-PLAN.md — Stale entry removal for unloaded components (gap closure)

**Details:**

Implemented thread-safe state management architecture:
- StateStore with single RwLock protecting all HashMaps (pins, signals, params)
- Read operations use lockShared() for concurrent access
- Write operations use lock() for atomic updates
- Refresh thread polls HAL at 100ms default interval
- Pubsub notification system with SubscriptionManager
- Condition variable with spurious wakeup handling
- Stale entry cleanup when components unload
- Lock hierarchy documented to prevent deadlock (never hold RwLock while calling HAL)

---

### Phase 3: TUI Core

**Goal**: Vaxis-based terminal interface with two-panel layout (tree navigation + data table) for browsing and viewing HAL components in real-time

**Depends on**: Phase 2

**Requirements**: TUI-01, TUI-02, TUI-03, TUI-04, TUI-05, TUI-06, TUI-07, TUI-08, CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-09, CORE-10, CORE-12

**Success Criteria** (what must be TRUE):
1. TUI displays two-panel layout: left panel for tree navigation, right panel for data table view
2. Tree view (left panel) shows component hierarchy with collapse/expand navigation and checkboxes to select items for display
3. Data table view (right panel) displays selected items in tabular format with columns: Name, Type, Direction, Current Value
4. Values update in real-time in data table cells at configured refresh rate without lag or stutter
5. User can search tree for pins/signals/params by name with glob pattern matching (e.g., "*pid*" shows all PID-related items)
6. User can filter table view by pin type (show only float pins, show only s32, etc.) and by component ownership (show all pins for pid.0)
7. TUI renders correctly on Raspberry Pi 5 terminal at 80x24 minimum resolution (panels scale appropriately)
8. Data table uses color or icon indicators to visually distinguish editable items from read-only items (only writable parameters and OUT/I/O pins are editable)
9. Input validation prevents type errors (numeric fields only accept numbers, cannot type invalid input)
10. Boolean/bit pins: pressing Enter toggles value (True ↔ False) without opening edit dialog; cell shows unsaved color while waiting for HAL refresh
11. Numeric pins: pressing Enter enables in-place editing in data table cell with validation (only valid numeric input accepted, Enter confirms, Escape cancels); cell shows unsaved color during editing
12. After edit confirmation (Enter): clear cell immediately and wait for next HAL refresh to display actual value (confirms write succeeded, applies to both numeric and boolean)
13. TUI displays helpful error messages for non-input-related invalid operations (e.g., attempting to edit read-only items)

**Plans**: 5 plans in 4 waves

Plans:
- [x] 03-00-PLAN.md — FFI write functions (pins and parameters)
- [x] 03-01-PLAN.md — Vaxis integration and two-panel layout
- [x] 03-02-PLAN.md — Tree view with component hierarchy
- [x] 03-03-PLAN.md — Data table with real-time updates
- [x] 03-04-PLAN.md — Search, filter, and in-place editing

**Details:**

Built complete Vaxis-based TUI with:
- Two-panel layout using vxfw SubSurface (30% tree, 70% table)
- Component hierarchy tree navigation with collapse/expand
- Data table with real-time value updates via pubsub
- Search with glob pattern matching (*pid* shows all PID items)
- Filter by pin type (bit/s32/u32/float)
- In-place editing for numeric values with validation
- Toggle for boolean values via Enter key
- Editability indicators via color coding
- Error message display with auto-clear after 5 seconds
- Help text showing all key bindings
- Responsive layout at 80x24 minimum resolution

---

### Phase 4: Configuration & Editing

**Goal**: Runtime HAL manipulation (edit parameters, create signals, link pins) and configuration persistence

**Depends on**: Phase 3

**Requirements**: CORE-07, CORE-08, CORE-11

**Success Criteria** (what must be TRUE):
1. User can edit writable parameter values in data table and changes are immediately reflected in HAL (equivalent to halcmd setp); only visually marked editable items can be edited
2. User can create new signals and link pins to them (equivalent to halcmd net command)
3. User can save current HAL configuration to file in halcmd-compatible format (can be loaded later or used as backup)

**Plans**: 3 plans in 3 waves

Plans:
- [x] 04-01-PLAN.md — FFI signal creation functions (halSignalNew, halLink, halUnlink)
- [x] 04-02-PLAN.md — TUI signal creation dialog with multi-step wizard
- [x] 04-03-PLAN.md — Configuration export module and save functionality

**Details:**

Implemented runtime HAL manipulation and configuration persistence:
- FFI wrappers for signal operations (halSignalNew, halLink, halUnlink)
- halSignalNew does NOT acquire mutex (C function handles locking internally)
- halLink/halUnlink acquire mutex explicitly following Phase 1 pattern
- LinkFailed and UnlinkFailed error types for specific error discrimination
- SignalDialog widget with 4-step wizard (name → type → pins → confirm)
- Type-safe pin filtering by HAL type (BIT/FLOAT/S32/U32)
- Multi-select pin pattern with Space key toggle
- StringHashMap(void) for selected pins (simpler than tracking full state)
- Configuration export module with halcmd-compatible output (net/setp commands)
- Save dialog with 's' key binding
- ArrayList(u8) for filename input (proper UTF-8 backspace handling)
- StateStore.pin_links HashMap infrastructure (full population deferred as v1 limitation)

**Known v1 Limitations:**
- Pin link tracking not fully populated (ULAPI signal pointer iteration too complex for v1)
- Export shows empty pin lists until refresh thread enhanced
- Save dialog visual rendering is placeholder (functionality complete, UI deferred)

---

## Milestone Summary

**Decimal Phases:**
None

**Key Decisions:**

From Phase 1 (FFI Foundation):
- Use -Dskip-hal-link build option for development without LinuxCNC
- Extern struct (not packed) for C ABI compatibility on ARM64
- Compile-time size assertions prevent silent ABI mismatches
- Error unions (!T) enforce explicit error handling
- Map LinuxCNC error codes to specific Zig error types
- Document memory ownership explicitly at FFI boundaries
- Write operations use HAL mutex lock/unlock, read operations are lock-free

From Phase 2 (State Management):
- Single RwLock per StateStore (not per HashMap) - simpler lock hierarchy
- Read operations use lockShared() for concurrent access
- Write operations use lock() for atomic updates
- Use .acquire/.release memory ordering for atomic flags
- Read HAL values BEFORE acquiring cache lock (prevents deadlock)
- Enumerate ALL pins each cycle via halpr_find_pin_by_name(null)
- Condition variable with has_changes predicate (prevents spurious wakeup bugs)
- waitForChange() uses while loop (not if) for correct spurious wakeup handling

From Phase 3 (TUI Core):
- Use vxfw framework (not low-level Vaxis API) - automatic redraw optimization
- Delegate layout logic to separate module (layout.zig)
- Use ctx.withConstraints for panel sizing - responsive layout
- Arena allocation pattern - ctx.arena for temporary allocations
- Two-panel layout uses SubSurface children with origin offsets
- Component hierarchy extracted from HAL item names using dot delimiter
- Cursor tracks position in visible_nodes list (not global tree)
- Use global variable (GLOBAL_REDRAW_FLAG) for pubsub callback access
- Determine editability via name heuristics for now
- Read values from StateStore cache (lock-free) instead of calling HAL FFI
- Pubsub-driven redraws - SubscriptionManager callbacks set redraw_flag
- glob.zig library used for pattern matching
- Search buffer managed via ArrayList(u8) for proper UTF-8 backspace handling
- Edit buffer is ArrayList with edit_mode flag controlling display
- Pending edits tracked in StringHashMap, displays "..." until refresh
- Error messages auto-clear after 5 seconds

From Phase 4 (Configuration & Editing):
- halSignalNew does NOT acquire mutex - C function handles locking internally
- halLink and halUnlink acquire mutex explicitly following Phase 1 pattern
- LinkFailed and UnlinkFailed error types for specific error discrimination
- Use StringHashMap(void) for selected pins - simpler than tracking full state
- Load available pins on type selection (not name input) - reduces HAL queries
- Copy pin names when selecting into StringHashMap - prevents dangling pointers
- Stub draw implementations for dialog steps - focus on functionality first
- Dialog lifecycle pattern: init() → open() → [wizard steps] → close() → deinit()
- Multi-select pattern with Space key toggle
- Defer pin link tracking in refresh thread - ULAPI signal pointer iteration is complex for v1
- Use ArrayList(u8) for save filename input - proper UTF-8 backspace handling
- Buffered file writing for export - use std.io.bufferedWriter for efficient I/O
- Null-terminate filename for std.fs.cwd API compatibility
- Placeholder for save dialog visual rendering - functionality complete, UI deferred

**Issues Resolved:**

From Phase 1:
- Project scaffolding with build.zig and proper Zig 0.15.1 API usage
- C header imports with lazy @cImport for development without hal.h
- ARM64 struct alignment verified with compile-time assertions
- Memory management at FFI boundaries documented

From Phase 2:
- Thread-safe cache access with RwLock preventing race conditions
- Deadlock prevention between HAL mutex and cache RwLock
- Spurious wakeup handling in condition variable
- Stale entry cleanup when components unload

From Phase 3:
- Responsive two-panel TUI layout using Vaxis framework
- Real-time value updates without blocking HAL refresh
- Tree navigation with component hierarchy
- In-place editing with proper validation
- Search and filter functionality

From Phase 4:
- Runtime HAL signal creation and pin linking
- Configuration export in halcmd-compatible format
- Multi-step dialog wizard pattern
- Type-safe pin filtering and selection

**Issues Deferred:**

- Pin link tracking in export (ULAPI signal pointer iteration too complex for v1)
- Save dialog visual rendering polish (functionality complete, UI enhancement deferred)
- Configuration restoration via halcmd -f (manual for v1, automated restore deferred)
- Read-only detection via direction field in cache (currently using name heuristics)
- Cursor selection in data table (edits first item, multi-row selection deferred)

**Technical Debt Incurred:**

- Pin link tracking infrastructure created but not populated (requires ULAPI signal iteration)
- Dialog draw functions are stub implementations (visual polish needed)
- Editability detection via name heuristics instead of cached direction field
- No cursor selection in data table (always edits first selected item)
- No automated config restore workflow (manual halcmd -f required)
- Some error messages could be more user-friendly

---
_For current project status, see .planning/ROADMAP.md_
